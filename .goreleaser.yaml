version: 2

project_name: shed-sidecar

before:
  hooks:
    - go mod tidy

builds:
  - id: shed-sidecard
    main: ./cmd/sidecard
    binary: shed-sidecard
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

  - id: shed-sidecar
    main: ./cmd/sidecarctl
    binary: shed-sidecar
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - id: binaries
    ids:
      - shed-sidecard
      - shed-sidecar
    files:
      - install.sh
      - README.md
    formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"
